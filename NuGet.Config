<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- This repository currently has no NuGet package dependencies. Keeping the
       source list empty makes local/offline builds reproducible. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
